import { HotelSearchSpa } from "@/components/search";

/**
 * Single Page Application entry — form + live SearchAPI results on one page.
 */
export default function HomePage() {
  return <HotelSearchSpa />;
}
